/**
 * Kintone グローバル型定義
 *
 * このファイルは、kintone-utilsパッケージをインポートしなくても
 * グローバル型定義（kintone.*, kintone.events.*等）を有効にするためのものです。
 *
 * @packageDocumentation
 */

import './kintone/global';
import './kintone/events';
